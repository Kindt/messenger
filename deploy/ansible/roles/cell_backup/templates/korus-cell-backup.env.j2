# Korus Cell backup env — {{ cell_backup_cell_id }}
# S3 credentials: Ansible vault / host env (never commit plaintext).
CELL_BACKUP_S3_ENABLED={{ cell_backup_s3_enabled | default('0') | string }}
{% if cell_backup_s3_endpoint is defined and cell_backup_s3_endpoint %}
CELL_BACKUP_S3_ENDPOINT={{ cell_backup_s3_endpoint }}
{% endif %}

---
# Stage / prod-like two-host inventory (TLS enabled via group_vars/all.yml).
all:
  children:
    korus_server:
      hosts:
        korus-server:
          ansible_host: stage-server.example.com
    korus_web:
      hosts:
        korus-web:
          ansible_host: stage-web.example.com

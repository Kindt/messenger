---
# Production two-host inventory (TLS + vault required).
all:
  children:
    korus_server:
      hosts:
        korus-server:
          ansible_host: prod-server.example.com
    korus_web:
      hosts:
        korus-web:
          ansible_host: prod-web.example.com

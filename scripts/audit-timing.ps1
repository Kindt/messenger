# Timing audit helper (epic 04) — compares exist vs missing resource latency.
param([string]$BaseUrl = "http://127.0.0.1:18080")
$ErrorActionPreference = "Stop"
Write-Host "Timing audit placeholder: run against live stack with representative UUIDs." -ForegroundColor DarkGray
Write-Host "See docs/SECURITY_AUDIT.md" -ForegroundColor DarkGray
exit 0

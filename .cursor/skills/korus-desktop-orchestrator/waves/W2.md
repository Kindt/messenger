# Wave W2 — Messaging parity + вложения + offline queue

**Matrix rows:** `chat-list`, `thread-messages`, `composer-send`, `read-receipts`, `mentions`, `threads`, `file-attach-download`, `offline-outgoing-queue`  
**Tasks:** DC-301 … DC-308

---

## SDK additions

| Class | Responsibility |
|-------|----------------|
| `ChatService` | list chats, messages, send, mark read |
| `AttachmentStore` | save to `{Downloads}/KorusMessenger/{profile}/{server}/{yyyy}/{mm}/` |
| `OutgoingQueue` | SQLite or JSON queue; retry with backoff |
| `MentionParser` | `@userId`, `@all` parity with server |
| `ThreadService` | `thread_id` filter on messages API |

### Attachment path (обязательно)

```text
{UserDownloads}/KorusMessenger/{profileDisplayName}/attachments/{serverSlug}/{yyyy}/{mm}/{fileId}-{safeFilename}
```

Sidecar: `{same}.meta.json` with serverId, chatId, sha256.

---

## JavaFX screens

| Screen | API |
|--------|-----|
| `chat-list.fxml` | `GET /api/v1/chats` per active server |
| `thread.fxml` | messages + composer + read receipts |
| `thread-sidebar.fxml` | thread replies (`thread_id`) |

Composer: attach file → upload → send message with attachment refs.

---

## Tests & smokes

```powershell
.\gradlew.bat :modules:desktop-client-sdk:test
.\scripts\smoke-desktop-messaging.ps1
```

Playwright web tiers **не заменяют** desktop smokes; API parity cross-check optional.

---

## DoD W2

- [ ] Send/receive message on QEMU
- [ ] Attachment lands in Downloads path
- [ ] Offline: queue 1 message, flush on reconnect
- [ ] Matrix rows W2 PASS

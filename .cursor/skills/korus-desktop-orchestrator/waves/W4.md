# Wave W4 — Updates, packaging, full parity gate

**Matrix rows:** `auto-update`; admin-console remains deferred  
**Tasks:** DC-501 … DC-505

---

## Update client

| Component | Implementation |
|-----------|----------------|
| `UpdateChecker` | fetch manifest per `contracts/update-manifest.schema.json` |
| `UpdateVerifier` | sha256 + Ed25519 signature |
| `UpdateInstaller` | platform: run MSI/dmg/deb or delegate to OS |

Settings: `update_channel`, `update_policy`, optional `update_feed_url` per profile.

### Corporate policy

If `update_feed_url` set in profile settings → override default feed.

---

## Packaging

```powershell
.\scripts\package-desktop-jpackage.ps1 -Platform windows-x64
```

Output: `build/desktop-dist/` (local, not committed).

---

## Full product DoD (весь spec 031)

- [ ] Waves W0–W4 qa-evidence PASS
- [ ] `feature-parity-matrix.json`: all `required` rows PASS or documented DEFER with ADR
- [ ] `buildIntegrity` PASS
- [ ] Manual checklist: multi-user profiles, multi-server, attachments path, update notify
- [ ] `pipeline-status.json`: `wave=W4`, `role=Done`, `product_delivery=COMPLETE`

---

## Smokes

```powershell
.\scripts\smoke-desktop-update-manifest.ps1
.\scripts\smoke-desktop-full-parity.ps1
```

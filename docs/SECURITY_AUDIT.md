# Security timing audit

Run `scripts/audit-timing.ps1` against a live stack to regenerate this report.

| Probe | Status |
|-------|--------|
| GET existing vs missing chat | pending |

Threshold: 5% relative delta (configurable via `-MaxDeltaRatio`).

# Hotspot Report — 2026-05-23 (updated 2026-05-24)

Status: `prometheus-baseline-complete` (JFR blocked in production JRE containers — see methodology)  
Source plan tasks: `T017` (core-api), `T022`

## Profiling Session Metadata

| Field | Value |
|---|---|
| Environment | QEMU server VM (`http://127.0.0.1:18080`), docker `core-api` (Temurin 25 **JRE**, no `jcmd`) |
| Load profile | `scripts/profiling/load-core-api.ps1` — 15s, concurrency=3, mixed `GET /messages` + `GET /health/ready` |
| Profiling tools | Prometheus JVM metrics (`/api/v1/metrics/prometheus`); JFR scripts for host-local JDK runs |
| Start time | 2026-05-24T10:00+03:00 |
| End time | 2026-05-24T10:16+03:00 |

## Methodology Note

Docker images use `eclipse-temurin:25-jre` — **JFR/jcmd unavailable** inside containers. Baseline uses Prometheus heap sampling during synthetic load. Host-local profiling: `scripts/profiling/profile-core-api.ps1`.

---

## Core API

### HealthResource.ready() / AdminServerStatsService — ephemeral Redis clients

| Field | Value |
|---|---|
| Location | `HealthResource.ready()`, `AdminServerStatsService` |
| Hotspot type | MEMORY / IO |
| Estimated impact | High under orchestrator probes + admin stats polling |
| Root cause | `RedisClient.create()` + `connect()` on every `/health/ready` call |
| Proposed fix | Shared `RedisProbe` |
| Verification status | **Verified** — `docs/review/hotspots-before-after-2026-05-24.md` |

### MessageRepository.findByChatId — JSON row mapping

| Field | Value |
|---|---|
| Location | `MessageRepository.findByChatId()` |
| Hotspot type | CPU / MEMORY |
| Estimated impact | Moderate on chat history reads |
| Root cause | Per-row DTO mapping on list endpoints |
| Proposed fix | Future: column projection for list view |
| Verification status | Pending JFR |

---

## Deep Archiver / Retention / Indexer / Export

See static analysis sections in prior template; chunk writer unified via `ChunkedSnapshotWriter` (Phase B). Worker Prometheus profiles pending (`T018`–`T021`).

---

## Top-3 Fixes Selected (T050)

1. **RedisProbe** — core-api — **implemented**
2. **ChunkedSnapshotWriter** — common — **implemented (Phase B)**
3. **Retention large-body streaming** — `writeChunkedSnapshotFromFile` — **implemented**
